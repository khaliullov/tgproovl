apiVersion: v1
kind: Service
metadata:
  name: tgproovl-svc
spec:
  type: ClusterIP
  selector:
    app: tgproovl-web
  ports:
  - protocol: TCP
    port: {{ .Values.servicePort }}
    targetPort: {{ .Values.port }}
